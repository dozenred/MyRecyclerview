<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.dozen.myrecyclerview">

    <application
        android:name=".appconfig.AppData"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"/>
        <activity android:name=".ui.activity.TestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.FruitRecyclerActivity"/>
        <activity android:name=".ui.activity.StudentRecyclerActivity"/>
        <activity android:name=".ui.activity.AsyncTaskActivity"/>
        <activity android:name=".ui.activity.OrdersActivity">
        </activity>
    </application>

</manifest>